<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - TUIfly Time-Off Tool</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .approval-card {
      background: white;
      border-radius: 15px;
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
      padding: 2rem;
      max-width: 500px;
      width: 100%;
      text-align: center;
    }
    .approval-icon {
      font-size: 4rem;
      color: #f59e0b;
      margin-bottom: 1rem;
    }
    .pulse {
      animation: pulse 2s infinite;
    }
    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.05); }
      100% { transform: scale(1); }
    }
    .user-info {
      background: #f8f9fa;
      border-radius: 8px;
      padding: 1rem;
      margin: 1.5rem 0;
    }
  </style>
</head>
<body>
  <div class="approval-card">
    <div class="approval-icon pulse">
      <i class="bi bi-hourglass-split"></i>
    </div>
    
    <h2 class="text-primary mb-3">Waiting for Approval</h2>
    
    <p class="text-muted mb-4">
      Your account has been created successfully! An administrator needs to approve your access before you can start using the TUIfly Time-Off Tool.
    </p>
    
    <div class="user-info">
      <h6 class="mb-2">Your Account Details:</h6>
      <p class="mb-1"><strong>Email:</strong> <%= user.email %></p>
      <% if (user.name) { %>
        <p class="mb-1"><strong>Name:</strong> <%= user.name %></p>
      <% } %>
      <% if (user.code) { %>
        <p class="mb-0"><strong>Code:</strong> <%= user.code %></p>
      <% } %>
    </div>
    
    <div class="alert alert-info" role="alert">
      <i class="bi bi-info-circle"></i>
      <strong>What happens next?</strong><br>
      Our administrators have been notified of your registration and will review your account shortly. You'll receive an email once your access is approved.
    </div>
    
    <div class="d-grid gap-2">
      <button class="btn btn-outline-primary" onclick="checkApprovalStatus()">
        <i class="bi bi-arrow-clockwise"></i>
        Check Status
      </button>
      
      <a href="/auth/logout" class="btn btn-outline-secondary">
        <i class="bi bi-box-arrow-right"></i>
        Sign Out
      </a>
    </div>
    
    <div class="mt-4">
      <small class="text-muted">
        Need help? Contact your administrator or the IT department.
      </small>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
async function checkApprovalStatus() {
  try {
    const response = await fetch('/auth/status');
    const result = await response.json();
    
    // FIXED: Add null check for result.user
    if (result.authenticated && result.user && result.user.canUseApp) {
      // User is now approved! Redirect to main app
      window.location.href = '/';
    } else {
      // Still pending
      const btn = document.querySelector('button[onclick="checkApprovalStatus()"]');
      const originalText = btn.innerHTML;
      btn.innerHTML = '<i class="bi bi-clock"></i> Still Pending...';
      btn.disabled = true;
      
      setTimeout(() => {
        btn.innerHTML = originalText;
        btn.disabled = false;
      }, 2000);
    }
  } catch (error) {
    console.error('Error checking approval status:', error);
    alert('Error checking status. Please try again.');
  }
}
  </script>
</body>
</html>